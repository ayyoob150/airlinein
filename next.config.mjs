/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['image.cnbcfm.com','d3.harvard.edu',"media.licdn.com","upload.wikimedia.org","*"],  // Add the domain here
    },
};

export default nextConfig;
